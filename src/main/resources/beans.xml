<?xml version="1.0" encoding="UTF-8"?>

<!--

This defines the httpClient that Zuul uses.
It currently specifies various timeouts and number of concurrent connections.
It's good if these are higher than the DoS timeouts and concurrent connections.
 
 -->

<beans xmlns = "http://www.springframework.org/schema/beans"
   xmlns:xsi = "http://www.w3.org/2001/XMLSchema-instance"
   xsi:schemaLocation = "http://www.springframework.org/schema/beans
   http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

	<bean id="requestConfigBuilder" class="org.apache.http.client.config.RequestConfig"
	    factory-method="custom">
	    <property name="socketTimeout" value="120000" /> 
	    <property name="connectTimeout" value="12000" /> 
	</bean>
	
	<bean id="requestConfig" factory-bean="requestConfigBuilder" factory-method="build" />
	
	<bean id="httpClientBuilder" class="org.apache.http.impl.client.HttpClientBuilder"
	    factory-method="create">
	    <property name="defaultRequestConfig" ref="requestConfig" />
	    <property name="maxConnTotal" value="400" />
		<property name="maxConnPerRoute" value="200" />	    
	</bean>
	
	<bean id="httpClient" factory-bean="httpClientBuilder" factory-method="build" />
</beans>